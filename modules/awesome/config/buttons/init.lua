return {
    client = require("buttons.client"),
    root = require("buttons.root"),
    taglist = require("buttons.taglist"),
    tasklist = require("buttons.tasklist"),
    titlebar = require("buttons.titlebar")
}
